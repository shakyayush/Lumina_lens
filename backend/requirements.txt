# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Gemini AI (embeddings + LLM duplicate detection + context answering)
google-genai>=1.0.0

# Numeric (lightweight — no torch/ML models needed)
numpy>=1.26.0

# Database
motor>=3.3.0
pymongo>=4.6.0

# Data validation
pydantic>=2.6.0

# Redis (for WebSocket pub/sub)
redis>=4.6.0

# YouTube transcripts
youtube-transcript-api>=0.6.0

# Environment variables
python-dotenv>=1.0.0

# LiveKit RTC (optional — only used if RTC_PROVIDER_URL is set)
livekit-api>=0.7.0
